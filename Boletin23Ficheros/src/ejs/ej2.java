package ejs;

import java.io.File;
import java.nio.file.Files;
import java.nio.file.Path;
import java.util.ArrayList;

public class ej2 {

	public static void main(String[] args) {
	/*	File fichero = new File("/home/alumno/hola.txt");
		ArrayList<String> lineas = numeroLineas(fichero);
		estadisticas(fichero);
		*/
	}
	
	/*public static void estadisticas(File fichero) {
		System.out.printf("Número de líneas: %d\n", lineas.size());
		System.out.printf("Líneas en blanco: %d\n", lineasEnBlanco(fichero));
		System.out.printf("Cantidad de caracteres sin contar los espacios: %d\n", caracteresTotales(fichero));
		System.out.printf("Cantidad de espacios: %d\n", espaciosTotales(fichero));
	}
	
	public static ArrayList<String> numeroLineas(File fichero) {
		ArrayList<String> contenido = null;
		Path ruta = Path.of(fichero);
		try {
			contenido = (ArrayList<String>) Files.readAllLines(ruta);
		} catch (Exception e) {
			System.out.printf("Error con el fichero %s\n", fichero);
			System.out.println(e.getMessage());
		}
		return contenido;
	}
	
	public static int lineasEnBlanco(File fichero) {
		return 0;
		
	}

	public static int caracteresTotales(File fichero) {
		return 0;
		
	}
	
	public static int espaciosTotales(File fichero) {
		return 0;
		
	}
*/
}

